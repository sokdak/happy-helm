apiVersion: v2
name: happy
description: Self-hosted happy (slopus/happy) — single-pod relay + web UI to control Claude Code / Codex
type: application
version: 0.1.0
# appVersion tracks the happy release pinned by the image (HAPPY_REF in docker/Dockerfile); bump per release.
appVersion: "2026.07.28"
home: https://github.com/sokdak/happy-helm
sources:
  - https://github.com/slopus/happy
keywords:
  - claude
  - codex
  - happy
  - self-hosted
maintainers:
  - name: sokdak
