# Deterministic overrides for `helm template` verification (not used in production).
image:
  tag: "test"
api:
  host: api.happy.example.com
fe:
  host: happy.example.com
postgres:
  storageClass: "test-sc"
